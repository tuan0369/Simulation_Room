# CLASSROOM DEMO DEFAULT — anonymous and plaintext by design.
# This is convenient for local teaching only and must not be exposed to an
# untrusted network. For an external deployment target, see
# mosquitto-hardened.conf and ../README.md.
allow_anonymous true

listener 1883
protocol mqtt

listener 9001
protocol websockets
